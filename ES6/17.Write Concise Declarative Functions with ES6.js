// Only change code below this line
const bicycle = {
    gear: 48,
    setGear(newGear){
      return {newGear}
    }
    
  };
  // Only change code above this line
  bicycle.setGear(3);
  console.log(bicycle.gear);